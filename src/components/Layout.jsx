import React from 'react'

const Layout = () => {
  return (
    <div className="relative w-full h-screen bg-gradient-to-t from-primary via-secondary to-primary text-vanilla"></div>
  )
}

export default Layout